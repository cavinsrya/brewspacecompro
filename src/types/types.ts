export interface ComproInfo {
  hero_image_url: string;
  title: string;
}

export interface MainContent {
  title: string;
  description: string;
  image_url: string;
}

export interface Testimonial {
  name: string;
  role: string;
  image_url: string;
  textreview: string;
}

export interface Product {
  name: string;
  image_url: string;
}

export interface TeamMember {
  name: string;
  position: string;
  image_url: string;
}
